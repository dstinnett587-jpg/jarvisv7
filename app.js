const stage=document.getElementById('stage'),status=document.getElementById('status'),responseEl=document.getElementById('response'),hint=document.getElementById('hint'),activateBtn=document.getElementById('activateBtn'),sitePreview=document.getElementById('sitePreview'),siteFrame=document.getElementById('siteFrame'),closePreview=document.getElementById('closePreview');
const MEMORY_KEY='alfred_conversation_v1';
function loadHistory(){try{const saved=JSON.parse(localStorage.getItem(MEMORY_KEY)||'[]');return Array.isArray(saved)?saved.slice(-30):[]}catch{return[]}}
function saveHistory(){try{localStorage.setItem(MEMORY_KEY,JSON.stringify(history.slice(-30)))}catch{}}
let history=loadHistory(),isAsking=false,isSpeaking=false,activated=false,persistentAudio=null,audioCtx=null,stream=null,recorder=null,followUpMode=false,followUpUntil=0,recordTimer=null,processingAudio=false,locationContext=null;
function setStatus(text,state=''){if(status)status.textContent=text;stage.classList.remove('listening','thinking','speaking');if(state)stage.classList.add(state)}
function showResponse(text=''){if(responseEl)responseEl.textContent=text}
function splitSpeech(text){const out=[];let rest=(text||'').replace(/\s+/g,' ').trim();while(rest.length){if(rest.length<=170){out.push(rest);break}let cut=Math.max(rest.lastIndexOf('. ',170),rest.lastIndexOf('? ',170),rest.lastIndexOf('! ',170),rest.lastIndexOf(', ',170),rest.lastIndexOf(' ',170));if(cut<70)cut=170;out.push(rest.slice(0,cut+1).trim());rest=rest.slice(cut+1).trim()}return out}
function ensureAudio(){if(!persistentAudio){persistentAudio=document.createElement('audio');persistentAudio.setAttribute('playsinline','');persistentAudio.preload='auto';persistentAudio.style.display='none';document.body.appendChild(persistentAudio)}return persistentAudio}
function ensureAudioContext(){if(!audioCtx){const AC=window.AudioContext||window.webkitAudioContext;if(AC)audioCtx=new AC()}return audioCtx}
async function unlockAudioContext(){const ctx=ensureAudioContext();if(!ctx)return false;try{if(ctx.state!=='running')await ctx.resume();const b=ctx.createBuffer(1,1,22050),s=ctx.createBufferSource();s.buffer=b;s.connect(ctx.destination);s.start(0);return true}catch(e){return false}}
async function playViaWebAudio(blob){const ctx=ensureAudioContext();if(!ctx)throw new Error('Web Audio unavailable');if(ctx.state!=='running')await ctx.resume();const buf=await blob.arrayBuffer(),decoded=await ctx.decodeAudioData(buf.slice(0));await new Promise((resolve,reject)=>{try{const src=ctx.createBufferSource();src.buffer=decoded;src.connect(ctx.destination);src.onended=resolve;src.start(0)}catch(e){reject(e)}})}
async function playViaElement(blob){const a=ensureAudio(),url=URL.createObjectURL(blob);try{a.pause();a.src=url;a.load();await new Promise((resolve,reject)=>{a.onended=resolve;a.onerror=()=>reject(new Error('Audio playback failed'));const p=a.play();if(p)p.catch(reject)})}finally{URL.revokeObjectURL(url)}}
async function playVoiceChunk(text){const r=await fetch('/api/tts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text})});if(!r.ok)throw new Error('Voice unavailable');const blob=await r.blob();try{await playViaWebAudio(blob)}catch{await playViaElement(blob)}}
function stopRecording(){clearTimeout(recordTimer);recordTimer=null;if(recorder&&recorder.state!=='inactive'){try{recorder.stop()}catch{}}recorder=null}
function blobToBase64(blob){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onloadend=()=>resolve(String(reader.result||'').split(',')[1]||'');reader.onerror=reject;reader.readAsDataURL(blob)})}
async function transcribe(blob){const audio=await blobToBase64(blob);const r=await fetch('/api/stt',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({audio,mime:blob.type||'audio/mp4'})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Transcription failed');return(d.text||'').trim()}
function requestLocation(){if(!navigator.geolocation)return;try{navigator.geolocation.getCurrentPosition(p=>{locationContext={latitude:+p.coords.latitude.toFixed(4),longitude:+p.coords.longitude.toFixed(4),accuracyMeters:Math.round(p.coords.accuracy||0),capturedAt:new Date().toISOString(),timeZone:Intl.DateTimeFormat().resolvedOptions().timeZone||null}},()=>{}, {enableHighAccuracy:false,maximumAge:600000,timeout:7000})}catch{}}
function isWeatherQuestion(text){return /\b(weather|temperature|temp|rain|raining|storm|snow|forecast|hot|cold|outside)\b/i.test(text)}
function websiteRequest(text){const m=text.match(/\b(?:build|make|create|design)\s+(?:a\s+)?website\s+(?:for\s+)?(.+)/i);return m?m[1].trim():null}
async function getWeatherReply(){if(!locationContext)return null;const r=await fetch('/api/weather',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({latitude:locationContext.latitude,longitude:locationContext.longitude})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Weather unavailable');return d.reply}
function openSitePreview(html){if(!sitePreview||!siteFrame)return;stopRecording();siteFrame.srcdoc=html;sitePreview.classList.add('open')}
function closeSite(){if(sitePreview)sitePreview.classList.remove('open');if(siteFrame)siteFrame.srcdoc='';if(activated&&!isAsking&&!isSpeaking)startAudioLoop(300)}
if(closePreview)closePreview.addEventListener('click',closeSite);
async function buildBusinessSite(details){setStatus('BUILDING WEBSITE…','thinking');const r=await fetch('/api/build-site',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({business:details})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Website build failed');openSitePreview(d.html);return `I built a concept website preview for ${details}. I opened it on your phone. Review it before you send or publish anything.`}
async function handleTranscript(said){said=(said||'').trim();if(!said)return;if(Date.now()<followUpUntil||followUpMode){followUpMode=false;followUpUntil=0;await ask(said);return}const m=said.match(/(?:\balfred\b|(?:^|[\s,.:;!?-])j(?=$|[\s,.:;!?-]))[\s,.:;!?-]*(.*)$/i);if(!m)return;const command=(m[1]||'').trim();if(command)await ask(command);else await speak('At your service.',{continueConversation:true})}
function startAudioLoop(delay=200){if(!activated||!stream||isAsking||isSpeaking||processingAudio||sitePreview?.classList.contains('open'))return;clearTimeout(recordTimer);recordTimer=setTimeout(()=>{if(!activated||isAsking||isSpeaking||processingAudio)return;const chunks=[];let mr;try{const preferred=['audio/mp4','audio/webm;codecs=opus','audio/webm'].find(t=>MediaRecorder.isTypeSupported?.(t));mr=preferred?new MediaRecorder(stream,{mimeType:preferred}):new MediaRecorder(stream)}catch{mr=new MediaRecorder(stream)}recorder=mr;mr.ondataavailable=e=>{if(e.data&&e.data.size)chunks.push(e.data)};mr.onstop=async()=>{recorder=null;if(!chunks.length){startAudioLoop(150);return}processingAudio=true;try{const blob=new Blob(chunks,{type:chunks[0]?.type||mr.mimeType||'audio/mp4'}),said=await transcribe(blob);await handleTranscript(said)}catch(e){console.warn('STT error',e)}finally{processingAudio=false;if(activated&&!isAsking&&!isSpeaking)startAudioLoop(120)}};mr.start();setStatus(Date.now()<followUpUntil?'LISTENING…':'LISTENING FOR “ALFRED” OR “J”…','listening');setTimeout(()=>{if(mr.state!=='inactive')mr.stop()},4200)},delay)}
function pickSystemVoice(){if(!('speechSynthesis'in window))return null;const voices=speechSynthesis.getVoices();return voices.find(v=>/Daniel|Arthur/i.test(v.name))||voices.find(v=>/en-GB/i.test(v.lang))||voices.find(v=>/^en/i.test(v.lang))||voices[0]||null}
async function systemSpeech(text){if(!('speechSynthesis'in window))throw new Error('System speech unavailable');const tracks=stream?.getAudioTracks?.()||[];tracks.forEach(t=>t.enabled=false);try{await new Promise(resolve=>{speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text),v=pickSystemVoice();if(v)u.voice=v;u.lang=v?.lang||'en-GB';u.rate=.9;u.pitch=.82;u.volume=1;u.onend=resolve;u.onerror=resolve;speechSynthesis.speak(u)})}finally{tracks.forEach(t=>t.enabled=true)}}
async function speak(text,{continueConversation=true}={}){if(!text)return;isSpeaking=true;stopRecording();setStatus('SPEAKING…','speaking');showResponse(text);try{for(const part of splitSpeech(text))await playVoiceChunk(part)}catch(e){try{await systemSpeech(text)}catch{}}finally{isSpeaking=false;if(continueConversation){followUpMode=true;followUpUntil=Date.now()+15000}else{followUpMode=false;followUpUntil=0}startAudioLoop(250)}}
async function ask(text){text=(text||'').trim();if(!text||isAsking)return;isAsking=true;stopRecording();setStatus('THINKING…','thinking');showResponse(text);try{let reply=null;const site=websiteRequest(text);if(site){reply=await buildBusinessSite(site)}else if(isWeatherQuestion(text)){if(!locationContext)requestLocation();for(let i=0;i<8&&!locationContext;i++)await new Promise(r=>setTimeout(r,250));if(locationContext)reply=await getWeatherReply()}if(!reply){const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,history:history.slice(-30),context:{location:locationContext,timeZone:Intl.DateTimeFormat().resolvedOptions().timeZone||null,localTime:new Date().toISOString()}})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Request failed');reply=d.reply}history.push({role:'user',content:text},{role:'assistant',content:reply});history=history.slice(-30);saveHistory();if(!sitePreview?.classList.contains('open'))await speak(reply,{continueConversation:true});else showResponse(reply)}catch(e){await speak('I had trouble with that request. Please try again.',{continueConversation:true})}finally{isAsking=false}}
async function activate(){if(activated)return;if(!navigator.mediaDevices?.getUserMedia||!window.MediaRecorder){setStatus('VOICE CAPTURE NOT SUPPORTED');return}if(activateBtn){activateBtn.disabled=true;activateBtn.textContent='Starting…'}try{await unlockAudioContext();if('speechSynthesis'in window){speechSynthesis.getVoices();const primer=new SpeechSynthesisUtterance('');primer.volume=0;speechSynthesis.speak(primer)}stream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true}});activated=true;requestLocation();if(activateBtn)activateBtn.classList.add('hidden');await speak(history.length?'ALFRED online. I remember our conversation.':'ALFRED online. At your service.',{continueConversation:true})}catch(e){activated=false;if(activateBtn){activateBtn.disabled=false;activateBtn.textContent='Start Alfred';activateBtn.classList.remove('hidden')}setStatus('MICROPHONE / AUDIO PERMISSION NEEDED')}}
if('speechSynthesis'in window)speechSynthesis.onvoiceschanged=()=>speechSynthesis.getVoices();if(activateBtn)activateBtn.addEventListener('click',activate);window.addEventListener('load',()=>setStatus('PRESS START ALFRED ONCE'));document.addEventListener('visibilitychange',()=>{if(document.hidden)stopRecording();else if(activated&&!isAsking&&!isSpeaking)startAudioLoop(300)});
