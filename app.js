const orb=document.getElementById('orb'),panel=document.getElementById('panel'),status=document.getElementById('status'),messages=document.getElementById('messages'),input=document.getElementById('input'),send=document.getElementById('send'),talk=document.getElementById('talk'),close=document.getElementById('close');
let history=[],recorder=null,stream=null,chunks=[];
function setStatus(x){status.textContent=x}
function add(text,cls='j'){const d=document.createElement('div');d.className='msg '+cls;d.textContent=text;messages.appendChild(d);messages.scrollTop=messages.scrollHeight}
function speak(text){if(!('speechSynthesis' in window))return;setStatus('SPEAKING…');speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='en-US';u.onend=()=>setStatus('READY • TYPE OR TALK');speechSynthesis.speak(u)}
async function ask(text){if(!text)return;add(text,'u');setStatus('THINKING…');send.disabled=true;try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,history:history.slice(-8)})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Request failed');history.push({role:'user',content:text},{role:'assistant',content:d.reply});add(d.reply);speak(d.reply)}catch(e){add('JARVIS error: '+e.message);setStatus('READY • TYPE OR TALK')}finally{send.disabled=false}}
async function startVoice(){try{if(!navigator.mediaDevices?.getUserMedia||!window.MediaRecorder)throw new Error('Voice recording is not supported in this browser.');stream=await navigator.mediaDevices.getUserMedia({audio:true});chunks=[];recorder=new MediaRecorder(stream);recorder.ondataavailable=e=>{if(e.data.size)chunks.push(e.data)};recorder.onstop=async()=>{stream.getTracks().forEach(t=>t.stop());talk.textContent='🎙️ Talk';setStatus('THINKING…');try{const f=new FormData();f.append('audio',new Blob(chunks,{type:recorder.mimeType}),'jarvis.webm');const r=await fetch('/api/voice',{method:'POST',body:f});const d=await r.json();if(!r.ok)throw new Error(d.error||'Voice request failed');await ask(d.text)}catch(e){add('Voice error: '+e.message);setStatus('READY • TYPE OR TALK')}};recorder.start();talk.textContent='⏹️ Stop';setStatus('LISTENING…')}catch(e){add(e.message);setStatus('READY • TYPE OR TALK')}}
orb.onclick=()=>{panel.classList.add('open');input.focus()};close.onclick=()=>panel.classList.remove('open');send.onclick=()=>{const t=input.value.trim();input.value='';ask(t)};input.onkeydown=e=>{if(e.key==='Enter')send.click()};talk.onclick=()=>recorder?.state==='recording'?recorder.stop():startVoice();
