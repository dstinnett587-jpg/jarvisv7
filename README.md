# JARVIS v7
Clean test build. Add AI provider keys only in Vercel Environment Variables. Text chat is the first test; voice endpoint is intentionally disabled until text chat works.

Repair branch deployment refresh after configuring the server-side provider key.

Preview refresh after updating Groq project scope.
