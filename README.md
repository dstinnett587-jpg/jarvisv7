# JARVIS v7
Clean test build. Add OPENAI_API_KEY only in Vercel Environment Variables. Text chat is the first test; voice endpoint is intentionally disabled until text chat works.

Repair branch deployment refresh after configuring the server-side API key.